# Import the sys module, which provides system-specific parameters and functions.
import sys

# Use the sys.version attribute to get the Python version you are using.
python_version = sys.version

# Print the Python version.
print("You are using Python version:")
print(python_version)

# import the sys module, access the sys.version attribute to get the Python version, and then print it to the console